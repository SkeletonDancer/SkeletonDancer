{
    "name": "{{ name }}",
    "description": "",
    "type": "{{ type }}",
    "license": "{{ license }}",
    "authors": [
        {
            "name": "{{ author.name }}",
            "email": "{{ author.email }}"
        }
    ],
    "require": {
        "php": ">={{ phpMin }}"
    },
    "autoload": {
        "psr-4": {
            "{{ namespace|escape_namespace }}\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "{{ namespace|escape_namespace }}\\Tests\\": "tests/"
        }
    }
}
