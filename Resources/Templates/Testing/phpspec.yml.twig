formatter.name: pretty

suites:
    {{ shortName }}:
        namespace: {{ namespace|normalizeNamespace }}
        psr4_prefix: {{ namespace|normalizeNamespace }}
        src_path: src/
        spec_prefix: spec\
